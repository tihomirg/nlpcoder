package commons.scores;

import commons.values.RankedValue;

public class RankedScore extends SingleScore<RankedValue> {

	public RankedScore(String name, RankedValue value){
		super(name, value);
	}
}
