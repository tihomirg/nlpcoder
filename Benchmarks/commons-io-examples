//Source: http://commons.apache.org/proper/commons-io/description.html

(1) Consider the task of reading bytes from a URL, and printing them.

 InputStream in = new URL( "http://commons.apache.org" ).openStream();
 try {
   System.out.println( IOUtils.toString( in ) );
 } finally {
   IOUtils.closeQuietly(in);
 }
 
 
 (2) Read an entire file line by line.
 
 File file = new File("/commons/io/project.properties");
 List lines = FileUtils.readLines(file, "UTF-8");
 
 
 (3) Normalize a filename removing double dot segments:
 
 String filename = "C:/commons/io/../lang/project.xml";
 String normalized = FilenameUtils.normalize(filename);
 // result is "C:/commons/lang/project.xml"
 
 
 (4) For example to find the free space on a drive:
 
 long freeSpace = FileSystemUtils.freeSpace("C:/");
 
 
 (5) Use LineIterator.
 
 LineIterator it = FileUtils.lineIterator(file, "UTF-8");
 try {
   while (it.hasNext()) {
     String line = it.nextLine();
     /// do something with line
   }
 } finally {
   LineIterator.closeQuietly(iterator);
 }
 

//Source: http://www.avajava.com/tutorials/lessons/how-do-i-write-a-string-to-a-file-using-commons-io.html


(1) Write a String to a File.
 
package test;

import java.io.File;
import java.io.IOException;

import org.apache.commons.io.FileUtils;

public class WriteStringToFile {

	public static void main(String[] args) throws IOException {
		String string = "This is\na test";
		File file = new File("test.txt");
		FileUtils.writeStringToFile(file, string);
	}
}


//Source: http://examples.javacodegeeks.com/core-java/apache/commons/io-commons/touch-file-using-apache-commons-io-example/

(1) Touch file.

File testFile = new File("test.txt");
FileUtils.touch(testFile);

//Source: http://stackoverflow.com/questions/13931392/how-to-continue-copying-on-error-with-copydirectory-from-fileutils-apache-commo

(1) Copy files from the folder srcDir to destDir.

String srcDir  = "/sourceDirectory/examples/";
String destDir = "/tmp/examples/";
try{
    FileUtils.copyDirectory(new File(srcDir), new File(destDir));
} catch (IOException e){
    e.printStackTrace();
}

(1) FileUtils.copyFile(sourceFile, targetFile);


---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------






